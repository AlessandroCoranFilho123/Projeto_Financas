<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.image.*?>
<?import javafx.scene.layout.*?>
<VBox xmlns="http://javafx.com/javafx/21"
      xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="app.controller.CartaoDialogController"
      styleClass="dialog-container"
      spacing="18"
      prefWidth="480">

    <padding>
        <Insets top="25" right="30" bottom="25" left="30"/>
    </padding>

    <HBox alignment="CENTER_LEFT" spacing="12">
        <ImageView fitHeight="36" fitWidth="36" preserveRatio="true" styleClass="dialog-logo">
            <Image url="@../icons/logo.png"/>
        </ImageView>
        <Label fx:id="lblTitulo" text="Nova compra no cartão" styleClass="view-title"/>
    </HBox>

    <VBox spacing="6">
        <Label text="Nome" styleClass="form-label"/>
        <TextField fx:id="txtNome" promptText="Ex: Notebook, mercado, curso" styleClass="form-field"/>
    </VBox>

    <VBox spacing="6">
        <Label text="Valor total" styleClass="form-label"/>
        <TextField fx:id="txtValor" promptText="0,00" styleClass="form-field"/>
    </VBox>

    <VBox spacing="6">
        <Label text="Meses parcelados" styleClass="form-label"/>
        <TextField fx:id="txtParcelas" promptText="1" styleClass="form-field"/>
    </VBox>

    <VBox spacing="6">
        <Label text="Vencimento da próxima parcela" styleClass="form-label"/>
        <DatePicker fx:id="dateVencimento"
                    promptText="Selecione a data"
                    prefHeight="40"
                    maxWidth="Infinity"
                    styleClass="form-combo"/>
    </VBox>

    <Label fx:id="lblErro"
           text=""
           visible="false"
           managed="false"
           styleClass="error-label"/>

    <HBox alignment="CENTER_RIGHT" spacing="10">
        <Button fx:id="btnExcluir"
                text="Excluir"
                visible="false"
                managed="false"
                styleClass="button-excluir"/>
        <Region HBox.hgrow="ALWAYS"/>
        <Button fx:id="btnCancelar" text="Cancelar" styleClass="button-secondary"/>
        <Button fx:id="btnSalvar" text="Salvar" styleClass="button-primary"/>
    </HBox>
</VBox>
